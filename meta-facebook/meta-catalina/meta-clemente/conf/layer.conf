# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "fb-clemente-layer"
BBFILE_PATTERN_fb-clemente-layer := "^${LAYERDIR}/"

LAYERSERIES_COMPAT_fb-clemente-layer := "whinlatter walnascar styhead scarthgap nanbield kirkstone"
