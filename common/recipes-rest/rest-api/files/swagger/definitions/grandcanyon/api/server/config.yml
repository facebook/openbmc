swagger: '2.0'
info:
  description: Swagger doc for /api/server/config
  title: Swagger doc for /api/server/config
  version: 1.0.0
paths:
  /api/server/config:
    get:
      produces:
      - application/json
      responses:
        200:
          description: ok
          schema:
            type: object
            additionalProperties: false
            properties:
              Information:
                type: object
                additionalProperties: false
                properties:
                  system_identify_server:
                    type: string
                  server_boot_order:
                    type: string
                  server_por_cfg:
                    type: string
                  sysfw_ver_server:
                    type: string
                  ntp_server:
                    type: string
                  server_sel_error:
                    type: string
                  server_sensor_health:
                    type: string
              Actions:
                type: array
                items:
                  type: string
              Resources:
                type: array
                items:
                  type: string
