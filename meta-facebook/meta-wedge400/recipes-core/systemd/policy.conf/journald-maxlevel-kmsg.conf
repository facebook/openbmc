# Suppress debug/info messages from being forwarded to the kernel
# console (serial). This reduces console spam from systemd-journald
# forwarding service log messages to /dev/kmsg.

[Journal]
MaxLevelKMsg=notice
