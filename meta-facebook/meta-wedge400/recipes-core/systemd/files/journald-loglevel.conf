# Reduce journald's own log verbosity to suppress debug messages
# like "Sent WATCHDOG=1 notification" and "Successfully sent stream
# file descriptor to service manager" from appearing on the console.

[Service]
Environment=SYSTEMD_LOG_LEVEL=notice
