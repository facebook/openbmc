# This file is purposefully empty; do not edit it.
#
# We must have a `${MACHINE}.conf` file here so that openbmc-init-build-env
# recognizes the machine, but the contents come from the upstream version.
#
# Overrides from upstream should be placed in this directory in
# a `${MACHINE}-upstream-overrides.conf` file
