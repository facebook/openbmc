# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "fb-santabarbara-layer"
BBFILE_PATTERN_fb-santabarbara-layer := "^${LAYERDIR}/"

LAYERSERIES_COMPAT_fb-santabarbara-layer := "walnascar styhead scarthgap nanbield kirkstone"
