The BMC shall support overriding the host boot source device via Redfish
and the management CLI. Supported targets include HDD, PXE, CD, Diags,
BiosSetup, and None. Override modes shall include one-time (Once),
persistent (Continuous), and disabled. Boot source override settings
shall persist across BMC reboots when set to Continuous mode.
