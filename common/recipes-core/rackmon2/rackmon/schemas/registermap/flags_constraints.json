{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "definitions": {
    "if": {
      "properties": {
        "format": {
          "const": "flags"
        }
      },
      "required": ["format"]
    },
    "then": {
      "required": ["flags"]
    },
    "else": {
      "not": {
        "required": ["flags"]
      }
    }
  }
}
