From f0b2762eaf37f4cc10309f23c2dfe2702e9aeb94 Mon Sep 17 00:00:00 2001
From: Igor Kanyuka <ifelmail@gmail.com>
Date: Mon, 2 Feb 2026 07:47:26 -0800
Subject: [PATCH 07/10] Subject: [PATCH] configure: lookup meson exutable from
 PATH

Upstream-Status: Inappropriate [workaround, would need a real fix for upstream]
---
 configure | 11 +----------
 1 file changed, 1 insertion(+), 10 deletions(-)

diff --git a/configure b/configure
index eea9030672..92a4bf4150 100755
--- a/configure
+++ b/configure
@@ -967,12 +967,6 @@ mkvenv="$python ${source_path}/python/scripts/mkvenv.py"
 $mkvenv ensuregroup --dir "${source_path}/python/wheels" \
      ${source_path}/pythondeps.toml meson || exit 1
 
-# At this point, we expect Meson to be installed and available.
-# We expect mkvenv or pip to have created pyvenv/bin/meson for us.
-# We ignore PATH completely here: we want to use the venv's Meson
-# *exclusively*.
-
-# for msys2
 get_pwd() {
     if pwd -W >/dev/null 2>&1; then
         pwd -W
@@ -981,10 +975,7 @@ get_pwd() {
     fi
 }
 
-meson="$(cd pyvenv/bin; get_pwd)/meson"
-if [ -f "$meson$EXESUF" ]; then
-  meson="$meson$EXESUF"
-fi
+meson=`which meson`
 
 # On Haiku, meson might show up in a "non-packaged" subfolder instead, see
 # https://github.com/haiku/haiku/blob/r1beta5/docs/user/storage/storageintro.dox
-- 
2.52.0

